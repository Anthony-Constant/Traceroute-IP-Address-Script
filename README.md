# Traceroute-IP-Address-Script
This PowerShell script automates the process of performing traceroute to multiple IP addresses, each in a new command prompt window, and logs the results to individual files.
